node_modules/
dist/
coverage/
uploads/*
!uploads/.gitkeep
pgdata/
dump.rdb
.env
*.log
# Local dev runtime (portable postgres/redis + data) — not for commit
.local-dev/
.env.local
.env.*.local

# Credentials / secrets — never commit
T.S & PR.S & PR.txt
*credentials*
*.credentials

# Separate standalone projects (own git repos) — must NOT be committed into this repo
abc-pr/
abc-production/
